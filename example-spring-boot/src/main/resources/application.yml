final:
  security:
    exclude-path: /login,/logout
    token-name-header-prefix: ""

spring:
  thymeleaf:
    cache: false
  http:
    encoding:
      charset: UTF-8
  jackson:
    time-zone: Asia/Shanghai

server:
  port: 8082