final:
  security:
    exclude-path: /login,/logout,/*
    use-view-session: true

spring:
  thymeleaf:
    cache: false
  http:
    encoding:
      charset: UTF-8
  jackson:
    time-zone: Asia/Shanghai

server:
  port: 8081